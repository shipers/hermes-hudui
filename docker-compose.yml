services:
  hermes-hudui:
    build: .
    ports:
      - "${PORT:-3001}:${PORT:-3001}"
    volumes:
      - ${HERMES_HOME:-~/.hermes}:/root/.hermes:ro
    environment:
      - HERMES_HOME=/root/.hermes
      - PORT=${PORT:-3001}
    restart: unless-stopped
